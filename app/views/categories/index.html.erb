<h3>Categories for Products we sell</h3>

<ul>
  <% @categories.each do |category| %>
    <li><%= link_to category.name, category %> has <%= pluralize(category.products.size, 'item') %> for sale.</li>

  <% end %>
</ul>
